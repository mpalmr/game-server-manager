import styled from 'styled-components';

const resetListStyles = `
  list-style: none;
  margin-top: 0;
  margin-bottom: 0;
  padding-left: 0;
`;

export const Ul = styled.ul`${resetListStyles}`;
export const Ol = styled.ol`${resetListStyles}`;
